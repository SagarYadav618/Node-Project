{>"layouts/master" /}

{<body}
{#doctor}
	<div class="large-3 columns book end">
    	<img src="{.cover}" style="width:200px", "height:400px">
    	<h4>{.title}</h4>
    	<p>{.truncText}</p>

    	<div class="price">Charges <span>RS{.price}</span></div>
    	<br>
    	<a href="/doctor/details/{._id}" class="button small">View Details</a>
    </div>
{/doctor}
{/body}
